/* Header generated by AMXX-Studio*/

#if defined _KnifeSystem_included
	#endinput
#endif
#define _KnifeSystem_included

enum _:eKnifeDatas
{
	sName,
	sVModel, 
	sPModel,
	fSpeed,
	fGravity,
	fDamage,
	iKnockback,
	iHealth,
	iVisibly,
	iLevel
}

enum _:ePlayerInfo
{
	iPlayerLvl,
	iPlayerXp,
	iPlayerPoints,
	iPlayerSelected,
	iPlayerSteamId[ 32 ]
};

/**
Visszatér a maximum elérhetõ szinttel.

	@return		A maximum elérhetõ szint.

*/
native ks_get_max_level( );

/**
Információkat kér le az adott azonosítójú játékosról.
Példa: ks_get_player_info( 3, iPlayerLvl )
	- lekéri a 3-as azonosítójú játékos szintjét.

	@param iPlayerId	A játékos azonosító.
	@param iPlayerInfo	Az infó amit le akarunk kérni. ( Lásd: enum iPlayerInfo )
	
	@return			Az infó értéke.

*/
native ks_get_player_info( iPlayerId, iPlayerInfo );

/**
Lekéri az adott id -jû késrõl az adott infót ( lásd: eKnifeDatas )
Stringnél szükséges megadni a tömböt amiben tárolni akarjuk.
Példa: ks_get_knife_info( sName, 5, sArray );
	- lekéri az 5 -s id-jû kés nevét az sArray változóba.
	
	@param iKnifeInfo	A lekérni kívánt kés infó. ( Lásd: eKnifeDatas )
	@param iKnifeId		A kés azonosítója
	@param ...		Ha karakterláncot kárnénk le ( 's' -el kezdõdik ), akkor
					a tömb amibe le akarjuk kérni és a mérete.
	
	@return			Lebegõpontos és intiger érték esetén az info értéke,
					karakterláncnál pedig 1
*/
native ks_get_knife_info( iKnifeInfo, iKnifeId, any:... );

/**
Leregisztál egy árut a boltba.
Példa: ks_register_item( "ExtraÉlet", "fnÉletMegvéve", 350 )
	- Leregisztrál egy árut, "ExtraÉlet" néven, ami 350 pontba kerül, és ha megveszi vki,
	  akkor az "fnÉletMegvéve" függvény fog meghívódni.
	  public fnÉletMegvéve( iVasarloId )

	  
	@param sItemName	Áru neve.
	@param sItemFunction	Áruhoz kapcsolt függvény.
	@param iCost		Áru ára.
	
	@return			Az áru id-je.
*/
native ks_register_item( const sItemName[ ], const sItemFunction[ ], iCost );

/**
Szintlépésekkor hívódik meg.

	@param iPlayerId	A szintet lépõ játékos azonosítóját adja át.
	@param iPlayerNewLvl	A szintet lépõ játékos új szintjét adja át.

	@note			Figyelmen kívül hagyja a visszatérési értéket.
*/
forward ks_level_up( iPlayerId, iPlayerNewLvl );

/**
Meghívódik miután elérte egy játékos a maximum szintet.

	@param iPlayerId	A maximum szintet elérõ játékos azonosítóját adja át.

	@note			Figyelmen kívül hagyja a visszatérési értéket.
*/
forward ks_max_lvl_reached( iPlayerId );

/**
Meghívódik miután a játékos sikeresen kiválasztott egy kést.

	@param iPlayerId	A kést kiválasztó játékos azonosítója.
	@param iKnifeId		A kiválasztott kés azonosítója.

	@note			Figyelmen kívül hagyja a visszatérési értéket.
*/
forward ks_knife_selected( iPlayerId, iKnifeId );
